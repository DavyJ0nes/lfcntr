{
	"name": "life-counter",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "tsc && vite build",
		"preview": "vite preview",
		"format": "biome format --write . && markdownlint-cli2 --fix '**/*.md' '#node_modules'",
		"check": "biome check . && markdownlint-cli2 '**/*.md' '#node_modules'"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.13",
		"markdownlint-cli2": "^0.20.0",
		"typescript": "~5.9.3",
		"vite": "npm:rolldown-vite@7.2.5"
	},
	"pnpm": {
		"overrides": {
			"vite": "npm:rolldown-vite@7.2.5"
		}
	}
}
